class Solution(object):
    def twoSum(self, numbers, target):
        left=0
        right= len(numbers)-1
        while left<right:
            sum_one=numbers[left]+numbers[right]
            if sum_one == target:
                return left+1,right+1
            elif sum_one>target:
                right-=1
            else:
                left+=1
