class Solution(object):
    def reverseString(self, s):
      def rev(left,right):
        if left >= right:
            return
        s[left],s[right] = s[right],s[left]
        rev (left+1,right-1)
      rev(0,len(s)-1) 
        
